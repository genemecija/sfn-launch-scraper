# sfn-launch-scraper
Python web scraper that scrapes spaceflightnow.com's launch schedule and outputs as JSON format.

### Built with:
* Python 3.8
* Libraries:
    * requests
    * BeautifulSoup4
    * json